import * as React from 'react';
import { NoteUI } from '@/components/luan/test/note/NoteUI';

const HomePage = () => {
  return (
    <div className="h-screen bg-white">
      <NoteUI />
    </div>
  );
};

export default HomePage;
